# Environment variables for Railway
# Add these in Railway Dashboard → Backend Service → Variables

# Use SQLite instead of PostgreSQL for now
DATABASE_URL=sqlite:///./data/jobs.db

# Scraper settings
SCRAPE_INTERVAL_HOURS=1

# CORS settings (replace with your actual Vercel URL)
# Include both production and preview deployment URLs
CORS_ORIGINS=https://faiz-lab.vercel.app,https://faiz-lab-git-automatic-job-postings.vercel.app,https://faiz-lab-*.vercel.app

# Port (Railway sets this automatically)
PORT=8001
